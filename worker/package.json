{
  "name": "flowcode-leaderboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker + D1 backing the flowcode daily leaderboard",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute flowcode --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute flowcode --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
